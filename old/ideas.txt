const lazyFunc = lazy(
  filter(v => v % 2),
  map(v => v * v)
)

lazyFunc([1, 2, 3, 4, 5])

1. Either(Left, Right) для манипуляции потоком исполнения
2. argFn для передачи сигнала по данным вверх (non-strict principle)
3. result type inference для получения нужных данных из потока


const map = (transform) => (next) => (arg) => {
  if (arg === left) {
    return next(arg)
  }

  const unwrapedArg = arg()

  return next(toArg({ ...unwrapedArg, value: transform(unwrapedArg.value) }))
}